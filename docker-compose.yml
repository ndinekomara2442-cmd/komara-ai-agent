# Komara AI Agent - Docker Compose
# docker-compose up -d

version: "3.8"

services:
  komara-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
    environment:
      - PORT=8000
      - SERVER_URL=https://your-domain.com
